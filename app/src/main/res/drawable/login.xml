<?xml version="1.0" encoding="utf-8"?>
<!--
    Legacy drawable left over from the original starter project.
    Nothing in the current code references it — kept around as a safety net
    in case Android Studio's build cache still has a stale reference to it
    after the UI redesign. Safe to delete after a clean rebuild.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="2dp"
        android:color="@color/brand_primary" />
    <corners android:radius="14dp" />
</shape>
